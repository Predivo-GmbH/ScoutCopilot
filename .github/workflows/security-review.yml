name: Security Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

# Collapse superseded runs: when several pushes land in a row only the
# newest commit needs testing. Safe here — these jobs never write to prod.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  security-review:
    # Auto-generated content changes (the SEO blog refresh, batched rewrites, dependency
    # bumps) are not code and do not need an engineering review. 41 of the 45 changes in the
    # 31 days to 2026-08-28 were exactly that, so reviewing them would be nearly all of the
    # cost and none of the value.
    if: >-
      github.event.pull_request.draft == false
      && !startsWith(github.head_ref, 'seo-auto-refresh/')
      && !startsWith(github.head_ref, 'seo-refresh-batch')
      && !startsWith(github.head_ref, 'seo/')
      && github.event.pull_request.user.login != 'dependabot[bot]'
      && github.event.pull_request.user.login != 'renovate[bot]'
    runs-on: ${{ vars.RUNNER_LABEL || 'ubuntu-latest' }}
    timeout-minutes: 15
    # A reviewer that cannot run must NOT report success. Until 2026-08-28 a missing key
    # only printed a warning and this job went green in 11 seconds having read nothing, so
    # changes were merged on the strength of a review that never happened. The key is now
    # installed on every repo carrying this workflow; if it ever goes missing, this fails
    # and says why.
    env:
      HAS_KEY: ${{ secrets.ANTHROPIC_API_KEY != '' }}
    permissions:
      contents: read
      pull-requests: write
      issues: write
      # claude-code-action@v1 exchanges an OIDC token for its GitHub App token, even when
      # an API key is supplied. Without this the action dies with "Could not fetch an OIDC
      # token" after ~30s and the gate goes red having reviewed nothing.
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Fail when the reviewer cannot run (no API key configured)
        if: env.HAS_KEY != 'true'
        run: |
          echo "::error::ANTHROPIC_API_KEY is not set on this repository, so this review cannot run."
          echo "A review that cannot run must not report success - that is how unreviewed changes get merged."
          exit 1

      - name: Mark the start of this review
        id: before
        run: echo "at=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

      - name: Run Security Review
        if: env.HAS_KEY == 'true'
        uses: anthropics/claude-code-action@v1
        with:
          # claude-code-action@v1 accepts `prompt` and `claude_args`. The old `model`,
          # `direct_prompt` and `allowed_tools` inputs are rejected ("Unexpected input(s)")
          # and the prompt was silently discarded, so this gate never reviewed anything.
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          # Post as the repo's own Actions bot. Without a token the action tries to mint
          # one from the Claude Code GitHub App and dies with "not installed on this
          # repository"; using GITHUB_TOKEN keeps the whole thing in version control and
          # grants no third-party app access to the org.
          github_token: ${{ secrets.GITHUB_TOKEN }}
          use_sticky_comment: true
          claude_args: --model claude-sonnet-4-6 --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*),Bash(git diff:*),Bash(git log:*),Bash(git show:*),Read,Glob,Grep"
          prompt: |
            BEFORE ANYTHING ELSE, understand how this run is judged.
            You MUST finish by posting to the pull request. Your reply text is discarded and
            nobody reads it. A run that posts nothing FAILS THE BUILD, even if the code is
            perfect - the job checks afterwards whether you said anything, because gates that
            passed silently were merging unreviewed code until 2026-08-28.
            - `gh pr comment` for the overall verdict. ALWAYS post this, every run, including
              runs where you have already reviewed this PR and nothing has changed, and runs
              where you find nothing at all. "No new findings since my last review" is a
              valid and useful verdict. Silence is not.
            - `mcp__github_inline_comment__create_inline_comment` (with `confirmed: true`) for
              each specific issue, anchored to file and line.
            - Post GitHub comments only. Do not return the review as your message text.

            You are a senior security engineer. Perform a security-focused code review
            to identify HIGH-CONFIDENCE vulnerabilities with real exploitation potential.

            CRITICAL:
            - Only flag issues where you are >80% confident of actual exploitability
            - Focus ONLY on security implications newly added by this PR
            - Do NOT report: DoS, secrets on disk, rate limiting, theoretical issues

            Categories to examine:
            1. **Input Validation**: SQLi, XSS, Command injection, Path traversal, XXE
            2. **Authentication & Authorization**: Missing auth, IDOR, privilege escalation
            3. **Crypto & Secrets**: Hardcoded secrets, weak crypto, insecure RNG
            4. **Code Execution**: RCE, unsafe deserialization, eval with user input
            5. **Data Exposure**: PII leakage, verbose errors, overly permissive CORS

            FALSE POSITIVE filters — NEVER flag:
            - Framework-provided protections (React XSS escaping, ORM parameterization)
            - Server-side only paths without user input
            - Test files and fixtures
            - TypeScript strict mode type-safe operations
            - Environment variables read at startup
            - Standard library crypto usage

            For each finding, trace the data flow from input to dangerous operation
            and verify no existing mitigation exists.
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

      # A reviewer that runs for three minutes and then says nothing leaves a GREEN check and
      # no review. That happened on signalscore on 2026-08-28 with the tools allowed and the
      # instruction given, so it is not left to the model's judgement. This passes only if the
      # bot said something during THIS run - a new comment or an updated one, so a reviewer
      # that keeps one running comment is fine and repeated pushes do not spam the thread.
      - name: A silent reviewer is a failed reviewer
        if: env.HAS_KEY == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          API: ${{ github.api_url }}/repos/${{ github.repository }}
          PR: ${{ github.event.pull_request.number }}
          SINCE: ${{ steps.before.outputs.at }}
        run: |
          spoke=0
          for ep in "issues/$PR/comments" "pulls/$PR/comments"; do
            n=$(curl -sS -H "Authorization: Bearer $GH_TOKEN" "$API/$ep?per_page=100" \
              | jq --arg s "$SINCE" '[.[]|select(.user.login=="github-actions[bot]")|select(.updated_at >= $s)]|length')
            spoke=$((spoke+n))
          done
          echo "reviewer comments created or updated since $SINCE: $spoke"
          if [ "$spoke" -eq 0 ]; then
            echo "::error::The reviewer ran but said nothing about this commit, so there is no review to read. A green check here would be a lie."
            exit 1
          fi
